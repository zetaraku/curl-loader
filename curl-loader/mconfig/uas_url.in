menu " UAS URL Settings"

config UAS_URL
	string " UAS URL"
          depends on UAS
	help
            UAS (user activity simulation) first URL.

config UAS_URL_USERNAME
	string " UAS URL Username"
          depends on UAS
	help
            Optional UAS URL username string. You may need it for web or proxy
            authentication.

config UAS_URL_PASSWORD
	string " UAS URL Password"
          depends on UAS
	help
            Optional UAS URL password string. You may need it e.g. for web or proxy
            authentication.

config UAS_URL_MAX_TIME
	int " UAS URL Maximum Time (Storming-only)"
	default 4
	range 1 1000
          depends on UAS
	help
	  Not used by the default smooth more. Adjust it for storming mode only.
	  The maximum time in seconds for UAS URL fetching. The parameter is used by the 
            storming mode to limit the download/upload time.

config UAS_URL_INTERLEAVE_TIME
	int " UAS URL Interleave Time"
	default 0
	range 0 3600000
          depends on UAS
	help
	  The time in milliseconds for client to sleep after fetching logoff URL prior to the 
            next URL to do. Used by the smooth mode and just ignored by the storming mode. 
            Zero (0) means don't wait and schedule client after the URL immediately.

endmenu
comment "  UAS URL Settings - support disabled"
	depends on !UAS
