menu " Logoff URL Settings"

config LOGOFF_URL
	string " Logoff URL"
          depends on LOGOFF
	help
            The Logoff URL.

config LOGOFF_URL_USERNAME
	string " Logoff URL Username"
          depends on LOGOFF
	help
            Optional logoff URL username string. You may need it for web or proxy
            authentication.

config LOGOFF_URL_PASSWORD
	string " Logoff URL Password"
          depends on LOGOFF
	help
            Optional logoff URL password string. You may need it e.g. for web or proxy
            authentication.

config LOGOFF_URL_MAX_TIME
	int " Logoff URL Maximum Time (Storming-only)"
          depends on LOGOFF
	default 4
	range 1 1000
	help
	  Not used by the default smooth more. Adjust it for storming mode only.
	  The maximum time in seconds for logoff URL fetching. The parameter is used by the 
            storming mode to limit the download/upload time.

config LOGOFF_URL_INTERLEAVE_TIME
	int " Logoff URL Interleave Time"
          depends on LOGOFF
	default 0
	range 0 3600000
	help
	  The time in milliseconds for client to sleep after fetching logoff URL prior to the 
            next URL to do. Used by the smooth mode and just ignored by the storming mode. 
            Zero (0) means don't wait and schedule client after the URL immediately.

endmenu
comment " Logoff URL Settings - support disabled"
	depends on !LOGOFF
