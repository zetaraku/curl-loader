menu " Logoff URL Settings"

config LOGOFF_URL
	string " Logoff URL"
          depends on LOGOFF
	help
            Logoff URL.

config LOGOFF_URL_USERNAME
	string " Logoff URL Username"
          depends on LOGOFF
	help
            Logoff URL username string

config LOGOFF_URL_PASSWORD
	string " Logoff URL Password"
          depends on LOGOFF
	help
            Logoff URL password string

config LOGOFF_URL_MAX_TIME
	int " Logoff URL Maximum Time"
          depends on LOGOFF
	default 1
	range 1 1000
	help
	  The maximum time for logoff URL fetching is used by the storming mode
            to limit the fetching time. 

config LOGOFF_URL_INTERLEAVE_TIME
	int " Logoff URL Interleave Time"
          depends on LOGOFF
	default 0
	range 0 3600000
	help
	  The time for client to sleep after fetching logoff URL prior to the next scheduling.
            Used by the smooth mode and just ignored by the storming mode. Valid values
            are 0 (don't wait) and 10-3600000.

endmenu
comment " Logoff URL Settings - support disabled"
	depends on !LOGOFF
